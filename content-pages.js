document.addEventListener('DOMContentLoaded',async()=>{const host=document.querySelector('[data-content-page]');if(!host)return;const key=host.dataset.contentPage;try{const data=await fetch('data/content-pages.json').then(r=>r.json());const rows=data[key]||[];host.innerHTML=rows.map((x,i)=>{const file=x.file?`<a class="content-file-action" href="${x.file}" target="_blank" rel="noopener">Open / Download →</a>`:'<span class="content-file-pending">File will be published here</span>';return `<article class="content-file-card"><div class="content-file-icon">${key==='gallery'?'▣':'▤'}</div><span>${x.type||x.date||'Update'}</span><h3>${x.title}</h3><p>${x.description||''}</p>${file}</article>`}).join('')||'<p>No published items yet.</p>'}catch(e){host.innerHTML='<p>Content is ready for publishing from the admin panel.</p>'}});